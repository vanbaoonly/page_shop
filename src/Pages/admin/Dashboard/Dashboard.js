import React from 'react';
import Logout from '../../../Components/Logout/Logout';

const Dashboard = () => {
    return (
        <div>
            Dashboard
            <Logout />
        </div>
    );
};

export default Dashboard;